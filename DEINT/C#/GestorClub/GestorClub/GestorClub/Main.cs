using System;
using GestorClub.Objetos;

namespace GestorClub {
class GestorClub {
    static void Main(string[] args) {
        // TODO: Inicializamos la GUI.
        // Inicializamos el fondo.
        Fondo fondo = new Fondo();
        InicializarDatos(fondo);
    }

    private static void InicializarDatos(Fondo fondo) {
        
    }
}
}