namespace GestorClub {
public class DataManager {
    /*
     * Esta clase se va a encargar de escribir y leer datos de un archivo .dat.
     * Los datos que se van a leer y escribir son registros, todos con el mismo tamaño.
     * El tamaño de los registros es concretamente de 
     */
}
}